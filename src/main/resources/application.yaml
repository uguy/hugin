spring:
  application.name: hugin
management:
  info:
    build:
      enabled: true
    git:
      enabled: true
    java:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
info:
  my-env-var: ${MY_VAR}